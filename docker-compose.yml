services:
  steel:
    image: ghcr.io/steel-foundation/steelmc:nightly
    stdin_open: true
    tty: true
    ports:
      - 25565:25565
    volumes:
      - ./config:/config
      - ./saves:/saves
      - ./logs:/.logs
