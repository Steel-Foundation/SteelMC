name: Build Release

on:
  push:
    branches:
      - master
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  version:
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.version.outputs.changed }}
      current: ${{ steps.version.outputs.current }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Check version change
        id: version
        shell: bash
        run: |
          previous=$(git show HEAD^:Cargo.toml | grep -m1 '^version' | sed 's/.*"\(.*\)".*/\1/')
          current=$(grep -m1 '^version' Cargo.toml | sed 's/.*"\(.*\)".*/\1/')

          echo "current=${current}" >> "$GITHUB_OUTPUT"
          if [[ "${previous}" == "${current}" ]]; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

  build:
    needs: version
    if: needs.version.outputs.changed == 'true'
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact: steel
          - os: windows-latest
            artifact: steel.exe
          - os: macos-latest
            artifact: steel
    steps:
      - uses: actions/checkout@v4
      - uses: Swatinem/rust-cache@v2
      - name: Build portable release binary
        env:
          RUSTFLAGS: ${{ matrix.rustflags }}
        run: cargo build --release --features stand-alone,deadlock_detection --verbose
      - uses: actions/upload-artifact@v4
        with:
          name: steel-${{ matrix.os }}
          path: target/release/${{ matrix.artifact }}

  release:
    needs:
      - version
      - build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release assets
        run: |
          mkdir -p release
          mv artifacts/steel-ubuntu-latest/steel release/steel-linux
          mv artifacts/steel-windows-latest/steel.exe release/steel-windows.exe
          mv artifacts/steel-macos-latest/steel release/steel-macos

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ needs.version.outputs.current }}
          name: v${{ needs.version.outputs.current }}
          target_commitish: ${{ github.sha }}
          draft: false
          prerelease: false
          files: release/*
          generate_release_notes: true

  notify:
    needs: release
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch to SteelDocs
        uses: peter-evans/repository-dispatch@v4
        with:
          token: ${{ secrets.DOCS_DISPATCH_TOKEN }}
          repository: Steel-Foundation/SteelDocs
          event-type: steel-released
